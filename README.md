# Epilepsy4D
This is the public repo which contains only the BUILT dist files from the 4DEC application. It must remain public so that we can host with Github pages, but because the files are built, the code is not modifiable.
